#include <iostream>
#include <Windows.h> /*  <Windows.h>:У цьому проекті він потрібен виключно для функцій
SetConsoleCP та SetConsoleOutputCP. Вони перемикають кодування консолі на Windows-1251,
щоб українські літери відображалися коректно */

#include "array_utils.h"
#include "matrix_utils.h"

using namespace std;

int main() {
    SetConsoleCP(1251);  // Встановлюємо кодову сторінку 1251 для вводу (CP) та виводу (OutputCP)
    SetConsoleOutputCP(1251);  // Це дозволяє програмі "розуміти" українську мову в консолі

    cout << "========== ЗАВДАННЯ 1 ==========" << endl;
    runTask1(); // Виклик логіки першого завдання (одномірні масиви)

    cout << "\n========== ЗАВДАННЯ 2 ==========" << endl;
    runTask2(); // Виклик логіки другого завдання (матриці)

    cout << "\n========== ЗАВДАННЯ 3 ==========" << endl;
    runTask3(); // Виклик логіки третього завдання (сортування символів)

    return 0;
}